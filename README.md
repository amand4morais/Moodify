<div align="center">

  <h1>Moodify</h1>

  <p>
    <strong>A modern mood tracking application built with React & Vite.</strong>
  </p>

  <p>
    <a href="#en-english">🇺🇸 English</a>&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;&nbsp;<a href="#pt-português">🇧🇷 Português</a>
  </p>

  <p>
    <img src="https://img.shields.io/badge/React-20232A?style=for-the-badge&logo=react&logoColor=61DAFB" alt="React" />
    <img src="https://img.shields.io/badge/Vite-646CFF?style=for-the-badge&logo=Vite&logoColor=white" alt="Vite" />
    <img src="https://img.shields.io/badge/Tailwind_CSS-38B2AC?style=for-the-badge&logo=tailwind-css&logoColor=white" alt="Tailwind CSS" />
    <img src="https://img.shields.io/badge/Zod-3068B7?style=for-the-badge&logo=zod&logoColor=white" alt="Zod" />
  </p>

</div>

<br />

---

<h2 id="en-english">🇺🇸 English</h2>

### 📖 About the Project

**Moodify** is a web application designed to help users track their moods. It is built using a modern frontend stack designed for performance and scalability.

**Key Technologies:**
* **Core:** [React](https://reactjs.org/)
* **Build Tool:** [Vite](https://vitejs.dev/) (Fast Hot Module Replacement)
* **Styling:** [Tailwind CSS](https://tailwindcss.com/) (Utility-first CSS)
* **Icons:** [Lucide React](https://lucide.dev/)
* **Validation:** [Zod](https://zod.dev/) (Schema validation)

### 🚀 Getting Started

Follow these instructions to get a copy of the project up and running on your local machine for development and testing purposes.

#### Prerequisites

Make sure you have **Node.js** installed on your machine.
* [Download Node.js](https://nodejs.org/) (Version 18+ recommended)

#### 📦 Installation

1.  **Extract the project:**
    Extract the `Moodify.rar` file and navigate to the project root in your terminal.

    ```bash
    cd Moodify/moodify
    ```

2.  **Install dependencies:**
    Install the required packages using npm (or yarn/pnpm).

    ```bash
    npm install
    ```

#### ⚙️ Configuration

The project uses environment variables. You need to set them up before running the app.

1.  Find the `.env.example` file in the root directory.
2.  Duplicate it and rename the copy to `.env`.

    ```bash
    cp .env.example .env
    ```
3.  Open the `.env` file and fill in the required variables (e.g., API Keys, Database URLs) if necessary.

#### 🏃‍♂️ Running the App

Start the development server:

```bash
npm run dev
```

The application should now be running at http://localhost:5173 (or the port shown in your terminal).

#### 🛠️ Build for Production
To build the app for production:

```bash
npm run build
```
This will generate the optimized files in the dist folder.

---

<h2 id="pt-português">🇧🇷 Português</h2>

### 📖 Sobre o Projeto

**Moodify** é uma aplicação web projetada para ajudar usuários a acompanhar seu humor. Ela foi construída utilizando uma stack frontend moderna, pensada para desempenho e escalabilidade.

**Principais Tecnologias:**
* **Core:** [React](https://reactjs.org/)
* **Ferramenta de Build:** [Vite](https://vitejs.dev/) (Hot Module Replacement rápido)
* **Estilização:** [Tailwind CSS](https://tailwindcss.com/) (CSS utilitário)
* **Ícones:** [Lucide React](https://lucide.dev/)
* **Validação:** [Zod](https://zod.dev/) (Validação de schemas)

### 🚀 Começando

Siga estas instruções para obter uma cópia do projeto em execução na sua máquina local para fins de desenvolvimento e testes.

#### Pré-requisitos

Certifique-se de que você tem o **Node.js** instalado na sua máquina.
* [Baixar Node.js](https://nodejs.org/) (Versão 18+ recomendada)

#### 📦 Instalação

1.  **Extraia o projeto:**
    Extraia o arquivo `Moodify.rar` e navegue até a raiz do projeto no seu terminal.

    ```bash
    cd Moodify/moodify
    ```

2.  **Instale as dependências:**
    Instale os pacotes necessários usando npm (ou yarn/pnpm).

    ```bash
    npm install
    ```

#### ⚙️ Configuração

O projeto utiliza variáveis de ambiente. Você precisa configurá-las antes de rodar a aplicação.

1.  Encontre o arquivo `.env.example` no diretório raiz.
2.  Duplique-o e renomeie a cópia para `.env`.

    ```bash
    cp .env.example .env
    ```
3.  Abra o arquivo `.env` e preencha as variáveis necessárias (por exemplo, chaves de API e URLs de banco de dados), se aplicável.

#### 🏃‍♂️ Rodando a Aplicação

Inicie o servidor de desenvolvimento:

```bash
npm run dev
```

#### 🛠️ Build para Produção
Para gerar a versão otimizada para produção:

```bash
npm run build
```
Isso irá gerar os arquivos otimizados na pasta dist.

<div align="center"> <p>Made with ❤️ by the Moodify Team (Amanda Morais Ribeiro e José Ernesto Marra Filho)</p> </div>
